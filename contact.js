function navbar() {
    // Opens the side navigation by setting its width to 250px
    document.getElementById("mySidenav").style.width = "250px";
}

function closeNavbar() {
    // Closes the side navigation by setting its width back to 0
    document.getElementById("mySidenav").style.width = "0";
}